langcode: en
status: true
dependencies:
  config:
    - field.field.node.article.body
id: node.article.body.default
label: 'Body Summary'
rule: llm_text_create_summary
input_mode: base
weight: 100
worker_type: field_widget_actions
entity_type: node
bundle: article
field_name: body
edit_mode: false
base_field: body
prompt: |-
  Based on the context text, write an engaging SEO meta description. Summarize the main topic compellingly to encourage users to click and read more.

  Constraints:

  Keep the length strictly between 120 and 155 characters (approximately 20 to 25 words).

  Use an active voice and clear language.

  Do not use clickbait, and do not include quotation marks.

  Context:
  {{ context }}
token: ''
plugin_config:
  automator_enabled: 1
  automator_rule: llm_text_create_summary
  automator_mode: base
  automator_base_field: body
  automator_prompt: |-
    Based on the context text, write an engaging SEO meta description. Summarize the main topic compellingly to encourage users to click and read more.

    Constraints:

    Keep the length strictly between 120 and 155 characters (approximately 20 to 25 words).

    Use an active voice and clear language.

    Do not use clickbait, and do not include quotation marks.

    Context:
    {{ context }}
  automator_token: ''
  automator_edit_mode: 0
  automator_label: 'Body Summary'
  automator_weight: '100'
  automator_worker_type: field_widget_actions
  automator_ai_provider: default_json
  automator_joiner: ''
  automator_joiner_other: ''
