langcode: en
status: true
dependencies:
  config:
    - field.field.node.article.tags
id: node.article.tags.default
label: 'Tags Default'
rule: llm_taxonomy
input_mode: base
weight: 100
worker_type: field_widget_actions
entity_type: node
bundle: article
field_name: tags
edit_mode: false
base_field: body
prompt: |-
  Based on the context text, select between 2 to 5 taxonomy terms that best describe the text. Each term must be a noun or adjective of 1 to 3 words. Return terms in title case only, with no hyphens, no punctuation, and no duplicate meanings.

  Context:
  {{ context }}
token: ''
plugin_config:
  automator_enabled: 1
  automator_rule: llm_taxonomy
  automator_mode: base
  automator_base_field: body
  automator_prompt: |-
    Based on the context text, select between 2 to 5 taxonomy terms that best describe the text. Each term must be a noun or adjective of 1 to 3 words. Return terms in title case only, with no hyphens, no punctuation, and no duplicate meanings.

    Context:
    {{ context }}
  automator_token: ''
  automator_edit_mode: 0
  automator_label: 'Tags Default'
  automator_weight: '100'
  automator_worker_type: field_widget_actions
  automator_ai_provider: default_json
  automator_clean_up: ''
  automator_search_similar_tags: 0
