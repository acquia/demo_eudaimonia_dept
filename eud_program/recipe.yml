name: 'Eudaimonia Program'
type: Recipe
description: 'Program content type + demo programs for the Department of Eudaimonia site template.'
recipes:
  - eud_common
  # Ships the categories terms these programs reference (must apply first).
  - eud_category
install:
  - node
config:
  strict: false
  actions:
    workflows.workflow.editorial:
      addNodeTypes:
        - program
